{
  "name": "pawcheck-otp-worker",
  "private": true,
  "version": "1.0.0",
  "description": "PawCheck email-OTP sign-in backend (Cloudflare Worker + Brevo) — replaces functions/ (Firebase Cloud Functions), which requires the Blaze billing plan to deploy.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "jose": "^5.9.6"
  }
}
