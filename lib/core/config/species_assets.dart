/// Canonical paths to the species-selector illustrations used on the
/// account setup screen's Dog/Cat cards. Change the artwork by overwriting
/// these two files in place (same filenames) — nothing else needs to
/// change. Only rename the files if you also update this class.
abstract final class SpeciesAssets {
  static const dog = 'assets/images/dogPic.png';
  static const cat = 'assets/images/catPic.png';
}
