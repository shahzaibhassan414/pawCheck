name: paw_check
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  firebase_core: 4.13.0
  firebase_auth: ^6.5.7
  cloud_firestore: ^6.8.0
  google_generative_ai: ^0.4.7
  image_picker: ^1.2.3
  google_fonts: ^8.2.1
  url_launcher: ^6.3.2
  http: ^1.2.2
  firebase_messaging: ^16.5.0
  device_info_plus: ^13.2.0
  android_id: ^0.5.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_native_splash: ^2.4.8
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Brand-mark artwork — see lib/core/config/brand_assets.dart for the
  # single source of truth on these paths (consumed by build-time tooling
  # below, which can't reference that Dart class directly). Species
  # illustrations (dog/cat) live separately in assets/images/ — see
  # lib/core/config/species_assets.dart.
  assets:
    - assets/branding/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# flutter_launcher_icons config — generates the iOS/Android app icon from
# a dedicated `app_icon.png`, deliberately separate from `BrandAssets.logo`
# (see lib/core/config/brand_assets.dart) used everywhere in-app. That
# in-app mark is transparent so it can sit on Home's AppBar, the violet
# Account Setup badge, etc.; `app_icon.png` instead has the app's cream
# background baked in, which is exactly what an icon needs (a transparent
# icon would just show through to the home screen's own wallpaper) but
# would show as a visible cream square if reused inside e.g. the violet
# circle badge. Already has no alpha channel at all, so no
# `remove_alpha_ios` stripping needed. Regenerate with
# `dart run flutter_launcher_icons` whenever this artwork changes.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/branding/app_icon.png"
  min_sdk_android: 21

# flutter_native_splash config — generates the native iOS/Android launch
# screen shown before the Flutter engine boots. `color`/`color_dark` match
# AppTheme's `_lightScaffold` (the deliberate near-white override, not the
# raw seed-derived surface) and
# `ColorScheme.fromSeed(seedColor: 0xFF7C3AED, brightness: dark).surface`
# from lib/core/theme/app_theme.dart, so the solid-color splash is
# indistinguishable from the app's own scaffold background and there's no
# visual "flash" at handoff. `image`/`image_dark` are BrandAssets.logo/
# logoDark — `logo`'s bold opaque violet reads clearly on the near-white
# light background, while `logoDark`'s pale ghost silhouette + violet
# pulse glyph was specifically designed for a dark backdrop. Regenerate
# with `dart run flutter_native_splash:create` whenever any of these
# change.
flutter_native_splash:
  color: "FAFAF7"
  color_dark: "151218"
  image: assets/branding/logo.png
  image_dark: assets/branding/logo_dark.png
  android_12:
    color: "FAFAF7"
    color_dark: "151218"
    image: assets/branding/logo.png
    image_dark: assets/branding/logo_dark.png
  # Project is iOS-first with Android as fast-follow (per CLAUDE.md); web is
  # explicitly out of scope. An untracked `web/` dir exists in this working
  # tree from an earlier, unrelated experiment, which made this package
  # write web splash assets by default — disabled here to avoid scope creep.
  web: false
